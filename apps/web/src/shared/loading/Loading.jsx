import React from 'react';
import { _loading, _bar, _fill } from './Loading.styled';

const Loading = () => {
  return (
    <_loading>
      <_bar>
        <_fill />
      </_bar>
    </_loading>
  );
};

export default Loading;
